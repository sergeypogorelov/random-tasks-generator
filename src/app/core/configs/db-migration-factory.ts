import { dbStoreNames } from '../constants/db-store-names';

export function dbMigrationFactory() {
  return {
    // 1: (db: IDBDatabase, transaction: IDBTransaction) => {
    //   transaction.objectStore(dbStoreNames.tag);
    // }
  };
}
